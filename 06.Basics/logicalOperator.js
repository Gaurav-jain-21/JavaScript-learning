// 1. -> Logical AND (&&)
//-> True if both the operands /boolean values are true, else false
const a = true;
const b= false;
const c= 4;
console.log(a&&b);
// 2. -> Logical OR (||)
//-> TRUE if either of the operands/ boolean values is true. evalutes to false if both are false.
console.log(a||b);
// 3. -> Logical NOT (!)
//-> True if the operands if false and vice-versa
console.log(!a);
